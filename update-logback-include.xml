<included>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>

        <encoder>
            <Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</Pattern>
        </encoder>
    </appender>

    <timestamp key="curDate" datePattern="yyyyMMdd"/>

    <appender name="FILE" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator>
            <key>trackingId</key>
            <defaultValue>unknown</defaultValue>
        </discriminator>
        <sift>
            <appender name="FILE-${trackingId}-${curDate}" class="ch.qos.logback.core.FileAppender">
                <file>${UPDATE_LOGBACK_FILENAME}-${curDate}-${trackingId}.log</file>
                <append>false</append>
                <layout class="ch.qos.logback.classic.PatternLayout">
                    <pattern>%date %level [%thread] %logger{35} [%file:%line] - %msg%n</pattern>
                </layout>
            </appender>
        </sift>
    </appender>

    <appender name="BIZFILE" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator>
            <key>trackingId</key>
            <defaultValue>unknown</defaultValue>
        </discriminator>
        <sift>
            <appender name="BIZFILE-${trackingId}-${curDate}" class="ch.qos.logback.core.FileAppender">
                <file>${UPDATE_LOGBACK_FILENAME}-${curDate}-${trackingId}-biz.log</file>
                <append>false</append>

                <filter class="dk.dbc.iscrum.utils.logback.filters.BusinessLoggerFilter" />

                <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                    <level>INFO</level>
                </filter>

                <layout class="ch.qos.logback.classic.PatternLayout">
                    <pattern>%date{HH:mm:ss.SSS} %level - %msg%n</pattern>
                </layout>
            </appender>
        </sift>
    </appender>

    <appender name="ERRFILE" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator>
            <key>trackingId</key>
            <defaultValue>unknown</defaultValue>
        </discriminator>
        <sift>
            <appender name="ERRFILE-${trackingId}" class="ch.qos.logback.core.FileAppender">
                <file>${UPDATE_LOGBACK_FILENAME}-${curDate}-${trackingId}-warn-err.log</file>
                <append>false</append>

                <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                    <level>WARN</level>
                </filter>

                <layout class="ch.qos.logback.classic.PatternLayout">
                    <pattern>%date %level [%thread] %logger{35} [%file:%line] - %msg%n</pattern>
                </layout>
            </appender>
        </sift>
    </appender>

    <root level="TRACE">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="BIZFILE"/>
        <appender-ref ref="ERRFILE"/>
    </root>
</included>
